/**
 * SectionHeader — cabeçalho reutilizável para todas as seções da Home.
 * Reutiliza a implementação já existente de SectionTitle para não duplicar código.
 */
export { SectionTitle as SectionHeader } from "./SectionTitle";
